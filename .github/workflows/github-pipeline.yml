name: Pull Request into Master, Main, Develop and Preprod

on:
    push:
        branches:
            - 'master'
            - 'develop'
            - 'preprod'
            - 'main'
            - 'priya_dev'

jobs:
    #First job
    install-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: Linting
              run: yarn lint
